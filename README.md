# Project 2 – Exploratory Data Analysis (EDA)
Run `pip install -r requirements.txt`, then `python eda_analysis.py`.
Files: dataset CSV, Python analysis script, Excel analysis workbook, report, and 6 charts.
